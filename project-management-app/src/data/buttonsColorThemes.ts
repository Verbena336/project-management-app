import { createTheme } from '@mui/material';

export const saveButtonTheme = createTheme({
  palette: {
    primary: {
      main: '#229820',
    },
  },
});

export const deleteButtonsTheme = createTheme({
  palette: {
    primary: {
      main: '#5D5D5D',
    },
    secondary: {
      main: '#D81616',
    },
  },
});
